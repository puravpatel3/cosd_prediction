streamlit==1.25.0  # Update Streamlit to the latest version
pandas==2.2.3      # Updated version compatible with Python 3.12
numpy              # Leave version unspecified to use the latest compatible version
scikit-learn==1.3.0
requests==2.31.0   # Should already be compatible
joblib==1.3.2
setuptools>=65.5.0  # Ensure setuptools is updated to include distutils
pip==24.2
wheel
